# Changesets

Add a changeset for every user-visible parser, data, or API change:

```sh
pnpm changeset
```

Dependency and data-only updates still need a changeset because they may change classification results.
